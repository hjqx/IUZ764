# IUZ764
# MADE BY ZQH AKA HJQX

```py
(*_, __, ___) = (lambda *__: ({} for [] in (*__,)))(
    [], (("", __ := 1)[((____ := (lambda: ([] > []))()))]), {}
)
(__ := (
        _[__ := (____)].__setitem__(
           *(b'\x82'),
            'g',
        )
        or print(_)
    )
)
```

```py
@print
@lambda _:_.__call__(b'\x82',*b'3')
def _ ( f , g ) -> (...,) :
    r= lambda*_:_
    (*_, __, ___) = (lambda *__: ({} for [] in (*__,)))(
        [], (("",__ := 1)[((____ := (lambda: ([] > []))()))]), {}
    )
    (__ := (
            _[__ := (____)].__setitem__(
               *r(*(r(*( *f,)))), g,
            ))
    )
    return _,locals()
```

```py
@print
@lambda*_:(lambda _:_)(*_)(b'\x82',*b'3')
def _ ( f , g ) -> ( r:= lambda*_:_, ) :

    (*_, __, ___) = (lambda *__: ({} for [] in (*__,)))(
        [], (("",__ := 1)[((____ := (lambda: ([] > []))()))]), {}
    )
    (__ := (
            _[__ := (____)].__setitem__(
               *r(*(r(*( *f,)))), g,
            ))
    )
    
    return r(*(*_,))
```

```py
@print
@lambda *_: (lambda _: _)(*_)("z", *b"r")
def _(f, g) -> (r := lambda *_: _,):
    (*_, __, ___) = (lambda *__: ({} for [] in (*__,)))(
        [], (("", __ := 1)[((____ := (lambda: ([] > []))()))]), {}
    )
    (
        __ := (
            _[__ := (____)].__setitem__(
                *r(*(r(*(*f,)))),
                g,
            )
        )
    )
    return (z := (lambda _: _)) and (lambda **_: _)(**z(*(r(*(*_,)))))
```
```py
{                           "notes": 
"""       i just discovered than it is possible to use 
 lambda expressions in decorator x) so i made this bad boy"""}

@print
@lambda *_: (lambda _: _)(*_)("z", "qh")
def _(f, g,) -> (r := lambda *_: _,):
    (*_, __, ___) = (
        lambda *__,: ({} for [] in (*__,)))(
        [], (("", __ := 1)[((____ := (lambda: ([] > []))()))]), {}
    )
    (
        __ := (
            _[__ := (____)].__setitem__(
                *r(*(r(*(*f,)))),
                g,
            ))
    )
    return (z := (lambda _: _)) and (lambda **_: _)(**z(*(r(*(*_,)))))
```
